# Use the public npm registry for the @fortawesome scope (free packages only).
# Overrides any machine-level .npmrc that points this scope at the private
# Font Awesome registry, so CI/Vercel can install without an auth token.
@fortawesome:registry=https://registry.npmjs.org/
